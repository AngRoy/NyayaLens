"""Adapters — the only layer that imports third-party SDKs.

All adapters implement protocols from `nyayalens.core._contracts`. `core/`
modules depend on the protocol, never on the concrete class.
"""
